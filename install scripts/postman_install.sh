# first make sure snapd is installed
echo 'first make sure snapd is installed'
sudo apt install snapd snapd-xdg-open

# Installing postman from snap
echo 'Installing postman from snap'
snap install postman


echo 'Postman installed succesfully'
